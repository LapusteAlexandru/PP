
name: B

on:
  repository_dispatch:
    types: [run-action-d]

jobs:
  action-d:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # Add your desired steps for Action D
      - name: Run Action B
        run: echo "Action B is running"
      - name: Display Payload Data
        run: |
          echo "Value 1: ${{ github.event.client_payload.value1 }}"
          echo "Value 2: ${{ github.event.client_payload.value2 }}"

