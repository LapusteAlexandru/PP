
name: B

on:
  push:
    branches: [ "master" ]
  repository_dispatch:
    types: 
      - run-action-d
      - run-action-z
jobs:
  action-d:
    runs-on: windows-latest
    steps:
      - name: Set Variable
        run: |
          if ( $env:github.event.action -eq "run-action-d" ){
            echo "ENV_VAR = D" >> $env:GITHUB_ENV
          }
      - name: Checkout code
        uses: actions/checkout@v2

      # Add your desired steps for Action D
      - name: Run Action B
        run: echo "Action B is running"
      - name: Use Environment Variable
        run: echo $env:ENV_VAR

